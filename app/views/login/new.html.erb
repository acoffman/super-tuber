<!DOCTYPE html>
<html>
  <head>
    <title>🥔 SuperTuber</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href='https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css' rel='stylesheet'>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body>
    <section class="section is-large">
      <div class="columns">
        <div class="column is-offset-two-fifths is-3">
          <% if flash[:error] %>
            <div class="notification is-danger is-light">
              Wrong
            </div>
          <% end %>
          <%= form_with url: login_path, method: :post, class: 'box' do |f| %>
            <div class="field">
              <div class="control">
                <%= f.text_field :username, placeholder: 'Username', class: 'input' %>
              </div>
            </div>
            <div class="field">
              <div class="control">
                <%= f.text_field :password, type: 'password', placeholder: 'Password', class: 'input' %>
              </div>
            </div>
            <div class="field is-grouped is-grouped-right">
              <%= f.submit "Login", class: 'button is-primary' %>
            </div>
          <% end %>
        </div>
      </div>
    </section>
  </body>
</html>
